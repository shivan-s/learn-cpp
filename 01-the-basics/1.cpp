// What does a compiler do?
// A compiler takes human readable code and turns this into object files.

// What does a linker do?
// A linker combines or "links" the object files into a executable program to run on a piece of hardware.
